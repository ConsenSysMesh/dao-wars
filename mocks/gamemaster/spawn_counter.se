extern body: [notify_of_turn]

data spawn_count

def send_turn_notification_to(address):
    address.notify_of_turn(as=body)

def notify_of_spawn(spawn):
    self.spawn_count += 1

def get_spawn_count():
    return(self.spawn_count)
